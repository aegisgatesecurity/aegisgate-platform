# Build outputs
bin/
# Generated code (was: gen/main_sso_integration.py, deleted D23)
gen/
dist/
build/
*.exe
*.exe~
*.dll
*.so
*.dylib
*.test
*.out
*.prof
*.test-binary
*.test-binary.*
# AegisGate platform binary (built from cmd/aegisgate-platform/ — see README "Building from source")
# Untracked in v3.2.0 Phase 7 (commit 9788e5e-era binary was the last tracked copy).
/aegisgate-platform
/aegisgate-platform-bin
aegisgate-platform-*
!/aegisgate-platform/  # never ignore the source directory itself

# Build tool test binaries (from go test ./tools/...)
/build-lens-extension
/tools/build-lens-extension/build-lens-extension
/tools/build-lens-extension/build-lens-extension.exe
/tools/test-extension/test-extension
/tools/test-extension/test-extension.exe
# Ignore stray README docs (old or competition versions)
README-OLD.md
README_COMPETITION.md
README_d7fcfeb_restored.md
# Coverage artifacts not covered by generic patterns
*.coverprofile
# Local‑only test artifacts & labs
testlab/
admin-server
# Coverage artifacts (additional patterns)
coverage.out
coverage_*.out
coverage/
coverage.html
coverage.txt
final_coverage.out
*coverage*.out
*coverage*.txt
*coverage*.html
sign-test.sh
CASE-STUDY-TEMPLATE.md
admin-server
.admin-server/

# Test coverage
coverage.out
coverage_*.out
coverage/

# Go
vendor/
.upstream/
*.sum
# Whitelist go.sum files (Go module checksums, NOT coverage summaries).
# *.sum above would otherwise exclude go.sum; we need it committed
# for reproducible CI builds.
!go.sum
!**/go.sum
!**/*/go.sum

# Environment and secrets
.env
.env.*
.env.local
.envrc
*.pem
*.key
*.crt
*.p12
*.keystore

# IDE
.vscode/
.idea/
*.swp
*.swo

# OS
.DS_Store
Thumbs.db

# =============================================================================
# INTERNAL / BUSINESS-SENSITIVE — NEVER FORCE-ADD (no `git add -f`)
# =============================================================================
# These directories contain AegisGate Security LLC's internal commercial
# strategy, operational plans, and draft legal documents. They are NOT for
# the public open-source repo. The pre-commit hook in .githooks/ blocks any
# attempt to add files under these paths. Working copies live in the
# /home/chaos/Desktop/AegisGate/ workspace, OUTSIDE this repository.
#
# If a doc becomes safe to publish, MOVE it to docs/ (or another public
# location) and re-commit it under its new path. Never `git add -f` from
# these directories. — AegisGate Security, 2026-06-07
# =============================================================================
plans/
CI_DEBT.md
plans-*.md
SPRINT*.md
BUSINESS*.md
PLAN*.md
ROADMAP-INTERNAL.md
legal-docs/

# Audit lab
audit-lab/
audit-results/

# Admin/Customer portals (not built yet)
cmd/admin/
cmd/customer-portal/

# Internal/DevOps (keep local only)
.coverage-exclude.txt
coverage.html
coverage.txt
sign-test.sh
CASE-STUDY-TEMPLATE.md
admin-server
.admin-server/

# CI Test Environment (security testing, local only)
docker-compose.test.yml

# Billing config (contains pricing data — loaded at runtime)
# Billing config (pricing data loaded at runtime — copy billing-config.example.json to billing-config.json)
billing-config.json
pkg/billing/billing-config.json
.env.example
*.md.v*-backup
test-extension/test-extension
tools/manual-test/manual-test

# Lens test data (large parquet files, regenerated by tests)
pkg/lenstest/testdata/

# Lens test corpus (auto-generated by regenerate.sh; contains
# synthetic secrets that trigger push protection)
pkg/lenstest/corpus/*_gen.go
pkg/lenstest/corpus/regenerate.sh

# =============================================================================
# OPSEC: Pricing, billing, and internal docs removed from public tracking
# Added 2026-07-23 — these must never be re-committed to the public repo
# =============================================================================

# Pricing and billing config (loaded at runtime, contains Stripe keys)
content/pricing.md
pkg/billing/billing-config.json

# Internal development reports
D2*-*.md
Sprint-*.md
SPRINT-*-*.md

# Internal sales/marketing strategy docs
docs/lens-to-platform-upsell.md
docs/a2a-implementation-roadmap.md
