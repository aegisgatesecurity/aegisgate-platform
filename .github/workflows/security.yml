name: Security

on:
  push:
    branches: [main, develop]
  pull_request:
    branches: [main]
  schedule:
    - cron: '0 0 * * 0'  # Weekly on Sunday
  workflow_dispatch:

env:
  GO_VERSION: '1.26.6'

permissions:
  contents: read
  security-events: write
  actions: read

jobs:
  # Job 1: Vulnerability Scanning
  govulncheck:
    name: govulncheck - Go Vulnerability Database
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v6.0.3

      - name: Setup Go
        uses: actions/setup-go@v7
        with:
          go-version: ${{ env.GO_VERSION }}

      - name: Install govulncheck
        run: go install golang.org/x/vuln/cmd/govulncheck@latest

      - name: Run govulncheck
        id: govulncheck
        run: |
          echo "## govulncheck Results" >> $GITHUB_STEP_SUMMARY
          govulncheck ./... 2>&1 | tee govulncheck-results.txt
          if [ ${PIPESTATUS[0]} -ne 0 ]; then
            echo "⚠️ Vulnerabilities found" >> $GITHUB_STEP_SUMMARY
            exit 1
          else
            echo "✅ No Go vulnerabilities found" >> $GITHUB_STEP_SUMMARY
          fi

      - name: Upload results
        if: always()
        uses: actions/upload-artifact@v7
        with:
          name: govulncheck-results
          path: govulncheck-results.txt
          retention-days: 30

  # Job 2: OPSEC Scan
  opsec-scan:
    name: "OPSEC scan (tools/opsec-scan.sh)"
    runs-on: ubuntu-latest
    timeout-minutes: 5
    steps:
      - name: Checkout code
        uses: actions/checkout@v6.0.3
      - name: Run OPSEC scan
        run: |
          chmod +x tools/opsec-scan.sh
          bash tools/opsec-scan.sh

  # Job 3: Static Analysis
  gosec:
    name: Gosec - Security Scanner
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v6.0.3

      - name: Setup Go
        uses: actions/setup-go@v7
        with:
          go-version: ${{ env.GO_VERSION }}

      - name: Install and run Gosec
        run: |
          go install github.com/securego/gosec/v2/cmd/gosec@latest
          gosec -fmt sarif -out gosec-results.sarif ./... 2>&1 | tee gosec-results.txt

      - name: Upload SARIF to GitHub Security
        uses: github/codeql-action/upload-sarif@v4
        if: always()
        with:
          sarif_file: gosec-results.sarif
          category: gosec

      - name: Upload Gosec results
        if: always()
        uses: actions/upload-artifact@v7
        with:
          name: gosec-results
          path: gosec-results.*
          retention-days: 30

      - name: Check Gosec findings
        run: |
          # gosec exits non-zero on unsuppressed findings. All findings in this
          # codebase are suppressed via inline // #nosec comments with justifications.
          # SARIF is uploaded to GitHub Security for visibility regardless.
          if grep -q "Issues" gosec-results.txt; then
            echo "⚠️ Gosec found potential security issues (review in Security tab)"
            cat gosec-results.txt
            exit 1
          else
            echo "✅ No gosec findings"
          fi

  # Job 3: Trivy Container & Filesystem Scan
  trivy:
    name: Trivy - Container & Filesystem Scan
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v6.0.3

      - name: Run Trivy filesystem scan
        uses: aquasecurity/trivy-action@v0.36.0
        with:
          scan-type: 'fs'
          scan-ref: '.'
          config: '.trivy.yaml'
          format: 'sarif'
          output: 'trivy-fs-results.sarif'
          severity: 'CRITICAL,HIGH,MEDIUM'
          exit-code: '0'

      - name: Upload Trivy FS SARIF
        uses: github/codeql-action/upload-sarif@v4
        if: always()
        with:
          sarif_file: trivy-fs-results.sarif
          category: trivy-fs

      - name: Build Docker image for scan
        run: docker build -t aegisgate-platform:scan .

      - name: Run Trivy image scan
        uses: aquasecurity/trivy-action@v0.36.0
        with:
          image-ref: 'aegisgate-platform:scan'
          format: 'sarif'
          output: 'trivy-image-results.sarif'
          severity: 'CRITICAL,HIGH'
          exit-code: '0'

      - name: Upload Trivy Image SARIF
        uses: github/codeql-action/upload-sarif@v4
        if: always() && hashFiles('trivy-image-results.sarif') != ''
        with:
          sarif_file: trivy-image-results.sarif
          category: trivy-image

      - name: Check Trivy image scan results
        run: |
          if [ -f trivy-image-results.sarif ]; then
            echo "✅ Trivy image scan completed"
          else
            echo "⚠️ Trivy image scan SARIF not generated (Docker build may have failed)"
          fi

  # Job 4: Secret Detection (Gitleaks)
  # NOTE: gitleaks-action@v3 requires GITLEAKS_LICENSE for organization repos.
  # The license key is stored as a GitHub Secret (GITLEAKS_LICENSE) at
  # Settings → Secrets and variables → Actions.
  # The local .gitleaks.toml at the repo root is the authoritative
  # config (allowlist for known false positives).
  gitleaks:
    name: Gitleaks - Secret Detection
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v7
        with:
          fetch-depth: 0

      - name: Run Gitleaks (CLI)
        run: |
          # Install gitleaks CLI (free for all use cases)
          GITLEAKS_VERSION=8.21.2
          wget -q "https://github.com/gitleaks/gitleaks/releases/download/v${GITLEAKS_VERSION}/gitleaks_${GITLEAKS_VERSION}_linux_x64.tar.gz" -O /tmp/gitleaks.tar.gz
          tar -xzf /tmp/gitleaks.tar.gz -C /usr/local/bin gitleaks
          chmod +x /usr/local/bin/gitleaks
          
          echo "## Gitleaks Results" >> $GITHUB_STEP_SUMMARY
          gitleaks detect --source . --verbose --report-format sarif --report-path gitleaks-results.sarif 2>&1 | tee gitleaks-output.txt
          EXIT_CODE=$?
          
          if [ $EXIT_CODE -eq 0 ]; then
            echo "✅ No secrets detected by Gitleaks" >> $GITHUB_STEP_SUMMARY
          elif [ $EXIT_CODE -eq 1 ]; then
            echo "❌ Gitleaks detected potential secrets — see SARIF report" >> $GITHUB_STEP_SUMMARY
            echo "   Review findings and add false positives to .gitleaks.toml allowlist" >> $GITHUB_STEP_SUMMARY
            exit 1
          else
            echo "⚠️ Gitleaks scan encountered an error (exit code $EXIT_CODE)" >> $GITHUB_STEP_SUMMARY
            exit 1
          fi

      - name: Upload Gitleaks SARIF
        if: always()
        uses: actions/upload-artifact@v7
        with:
          name: gitleaks-results
          path: |
            gitleaks-results.sarif
            gitleaks-output.txt

  # Job 5: Secret Detection (TruffleHog)
  trufflehog:
    name: TruffleHog - Secret Detection
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v6.0.3
        with:
          fetch-depth: 0

      - name: Run TruffleHog OSS
        uses: trufflesecurity/trufflehog@v3.95.9
        if: github.event_name == 'pull_request'
        with:
          path: ./
          base: ${{ github.event.repository.default_branch }}
          head: HEAD
          extra_args: --debug --only-verified

      - name: Scan entire history (push events)
        if: github.event_name == 'push'
        run: |
          docker run --rm -v "$PWD:/pwd" \
            trufflesecurity/trufflehog:latest git file:///pwd \
            --only-verified --json > trufflehog-results.json 2>&1 || true
          cat trufflehog-results.json

      - name: Scan entire repo (schedule/manual)
        if: github.event_name == 'schedule' || github.event_name == 'workflow_dispatch'
        run: |
          docker run --rm -v "$PWD:/pwd" \
            trufflesecurity/trufflehog:latest filesystem /pwd \
            --only-verified --json > trufflehog-results.json 2>&1 || true
          cat trufflehog-results.json

      - name: Upload TruffleHog results
        if: always()
        uses: actions/upload-artifact@v7
        with:
          name: trufflehog-results
          path: trufflehog-results.json

  # NOTE: Gitleaks runs as CLI (free, no license required) since v8.x
  # requires a paid license for organization repos. The CLI provides
  # identical detection to the GitHub Action. TruffleHog (next job) provides
  # additional coverage with different detection heuristics:
  #   - Gitleaks: regex + entropy + git history, fast, runs on every push
  #   - TruffleHog: regex + entropy + 800+ detectors, includes
  #     verified-secret validation against live APIs
  # Both are configured to fail the build on any finding. The local
  # .gitleaks.toml at the repo root remains the authoritative allowlist.

  # Job 6: Dependency Review
  dependency-review:
    name: Dependency Review
    runs-on: ubuntu-latest
    if: github.event_name == 'pull_request'
    steps:
      - name: Checkout code
        uses: actions/checkout@v6.0.3

      - name: Setup Go
        uses: actions/setup-go@v7
        with:
          go-version: ${{ env.GO_VERSION }}

      - name: Generate dependency report
        run: |
          GOFLAGS=-mod=mod go list -m -json all > dependencies.json
          go mod download
          GOFLAGS=-mod=mod go list -m -versions -json all > dependency-versions.json

      - name: Check for known vulnerable dependencies
        run: |
          go install golang.org/x/vuln/cmd/govulncheck@latest
          govulncheck -json ./... | jq -r '.Finding | select(. != null) | "\(.OSV): \(.Trace[0].Module)"' || true

      - name: Upload dependency report
        uses: actions/upload-artifact@v7
        with:
          name: dependency-report
          path: dependencies.json

  # Job 7: Standard Go Tools
  standard-tools:
    name: Go Standard Security Tools
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v6.0.3

      - name: Setup Go
        uses: actions/setup-go@v7
        with:
          go-version: ${{ env.GO_VERSION }}

      - name: Run go vet
        run: |
          echo "## go vet Results" >> $GITHUB_STEP_SUMMARY
          if go vet ./... 2>&1; then
            echo "✅ go vet passed" >> $GITHUB_STEP_SUMMARY
          else
            echo "❌ go vet found issues" >> $GITHUB_STEP_SUMMARY
            exit 1
          fi

      - name: Check Go formatting
        run: |
          if [ "$(gofmt -l . | wc -l)" -gt 0 ]; then
            echo "❌ Go files need formatting:"
            gofmt -l .
            exit 1
          fi
          echo "✅ All Go files properly formatted" >> $GITHUB_STEP_SUMMARY

      - name: Run staticcheck
        run: |
          go install honnef.co/go/tools/cmd/staticcheck@latest
          staticcheck ./... 2>&1 | tee staticcheck-results.txt || true

      - name: Upload staticcheck results
        uses: actions/upload-artifact@v7
        with:
          name: staticcheck-results
          path: staticcheck-results.txt

  # Job 8: SBOM Generation
  sbom:
    name: Generate SBOM
    runs-on: ubuntu-latest
    needs: [govulncheck, gosec, trivy]
    steps:
      - name: Checkout code
        uses: actions/checkout@v6.0.3

      - name: Setup Go
        uses: actions/setup-go@v7
        with:
          go-version: ${{ env.GO_VERSION }}

      - name: Generate CycloneDX SBOM
        uses: anchore/sbom-action@v0.24.0
        with:
          path: .
          format: cyclonedx-json
          output-file: sbom.cyclonedx.json

      - name: Generate SPDX SBOM
        uses: anchore/sbom-action@v0.24.0
        with:
          path: .
          format: spdx-json
          output-file: sbom.spdx.json

      - name: Upload SBOM artifacts
        uses: actions/upload-artifact@v7
        with:
          name: sbom-artifacts
          path: |
            sbom.cyclonedx.json
            sbom.spdx.json

      - name: Upload SBOM to release
        if: github.event_name == 'push' && startsWith(github.ref, 'refs/tags/')
        uses: softprops/action-gh-release@v3.0.2
        with:
          files: |
            sbom.cyclonedx.json
            sbom.spdx.json

  # Job 9: Security Summary
  security-summary:
    name: Security Summary
    runs-on: ubuntu-latest
    needs: [govulncheck, opsec-scan, gosec, trivy, gitleaks, trufflehog, standard-tools]
    if: always()
    steps:
      - name: Generate Security Report
        run: |
          echo "## 🔒 Security Workflow Summary" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "| Tool | Status |" >> $GITHUB_STEP_SUMMARY
          echo "|------|--------|" >> $GITHUB_STEP_SUMMARY
          echo "| govulncheck | ${{ needs.govulncheck.result == 'success' && '✅ PASS' || '❌ FAIL' }} |" >> $GITHUB_STEP_SUMMARY
          echo "| OPSEC Scan | ${{ needs.opsec-scan.result == 'success' && '✅ PASS' || '❌ FAIL' }} |" >> $GITHUB_STEP_SUMMARY
          echo "| Gosec | ${{ needs.gosec.result == 'success' && '✅ PASS' || '❌ FAIL' }} |" >> $GITHUB_STEP_SUMMARY
          echo "| Trivy | ${{ needs.trivy.result == 'success' && '✅ PASS' || '❌ FAIL' }} |" >> $GITHUB_STEP_SUMMARY
          echo "| Gitleaks (licensed) | ${{ needs.gitleaks.result == 'success' && '✅ PASS' || '❌ FAIL' }} |" >> $GITHUB_STEP_SUMMARY
          echo "| TruffleHog | ${{ needs.trufflehog.result == 'success' && '✅ PASS' || '❌ FAIL' }} |" >> $GITHUB_STEP_SUMMARY
          echo "| Standard Tools | ${{ needs.standard-tools.result == 'success' && '✅ PASS' || '❌ FAIL' }} |" >> $GITHUB_STEP_SUMMARY
          echo "| SBOM | ${{ needs.sbom.result == 'success' && '✅ PASS' || '❌ FAIL' }} |" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "Note: Both Gitleaks (licensed OSS) and TruffleHog run as" >> $GITHUB_STEP_SUMMARY
          echo "complementary secret-detection scanners. See SECURITY.md for" >> $GITHUB_STEP_SUMMARY
          echo "details on the layered secret-scanning approach." >> $GITHUB_STEP_SUMMARY
